version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gosec
    - revive
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - gosec
          - errcheck
