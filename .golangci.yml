run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - misspell
    - gosec
    - revive
issues:
  exclude-rules:
    - path: _test\.go
      linters: [gosec, errcheck]
